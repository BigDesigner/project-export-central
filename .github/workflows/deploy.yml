name: Deploy to Cloudflare

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy Worker and Pages
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Root Dependencies
        run: npm ci

      # --- 0. Export Old D1 Database (mapassign) ---
      - name: Export Old D1 Database
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: '.'
          command: d1 export mapassign --remote --output=./old_db_dump.sql
        continue-on-error: true

      # --- 0.1 Clean and Prepare Migration Dump ---
      - name: Clean Migration Dump
        run: python3 scripts/prepare_migration_dump.py

      # --- 0.2 Import Old Data into New D1 (tenderpulse-db) ---
      - name: Import Migrated Data into New D1
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: '.'
          command: d1 execute DB --remote --file=./clean_migration.sql
        continue-on-error: true

      # --- 1. Migrate Production Database ---
      - name: Migrate Database (D1)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: '.'
          command: d1 execute DB --remote --file=./schema.sql

      # --- 1.1 Seed Tender Intelligence Data (D1) ---
      - name: Seed Tender Module Data (D1)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: '.'
          command: d1 execute DB --remote --file=./tender_seed.sql

      # --- 2. Deploy Cloudflare Worker (Backend) ---
      - name: Deploy Worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: '.'
          command: deploy

      - name: Build Frontend
        run: npm run frontend:build

      - name: Deploy Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: 'tenderpulse-frontend'
          directory: 'frontend/dist'
          branch: 'main'
